<%= form_for([@item, @item.comments.build]) do |f| %>

  <p>
    <%= f.label :post %><br>
    <%= f.text_area :post %>
  </p>
  <p>
      <button type="submit" name="action">Submit</button>
  </p>
<% end %>
