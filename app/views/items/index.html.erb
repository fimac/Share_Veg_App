<h1>List of Food Items Available</h1>

<!-- This is the list of food items  -->

  <% @all_items.each do |item| %>
    <div>
      <p><%= item.name %></p>
      <p><%= item.description %></p>
      <%= link_to(image_tag(item.image), "/items/#{item.id}") %>
      <p>Placeholder for user profile name and link to</p>
    </div>
  <% end %>
